# manban — Beispiel-Umgebung. Kopiere nach .env und passe die Werte an.
# .env NICHT einchecken (steht in .gitignore).
#
# ─────────────────────────────────────────────────────────────────────────────
# PRODUKTION (Hostinger, hinter Traefik) — siehe docs/deployment-hostinger.md.
# Der Prod-Stack startet mit:
#   docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
# Dort terminiert Traefik das TLS für kanban.mwolff.org; MANBAN_DOMAIN/MANBAN_ACME_EMAIL
# (die lokalen Caddy-Werte) werden NICHT verwendet. Für Produktion mindestens setzen:
#   MANBAN_BASE_URL=https://kanban.mwolff.org
#   MANBAN_SESSION_SECRET=<openssl rand -hex 32>   (Deploy bricht sonst ab — sowohl der
#     Compose-Aufruf als auch, sobald er das Netz erreicht, der Start der Anwendung selbst)
#   MANBAN_COOKIE_SECURE=true
#   POSTGRES_PASSWORD / MINIO_ROOT_USER / MINIO_ROOT_PASSWORD = starke Werte
#   MANBAN_MAIL_ENABLED=true + echte Strato-SMTP-Zugangsdaten
#   MANBAN_BOOTSTRAP_ADMIN_TOKEN=<Zufallswert> (nach dem ersten Admin wieder leeren)
#
# ⚠️  SONDERZEICHEN IN SECRETS: Docker Compose interpoliert `$` in .env-Werten.
#     Enthält ein Wert ein `$` (z. B. ein vorgegebenes SMTP-Passwort), muss JEDES
#     `$` als `$$` geschrieben werden — sonst wird der Wert stillschweigend
#     verstümmelt (Symptom: `WARN The "…" variable is not set`).
#     Beispiel: Passwort `ab$cd` → `ab$$cd` in der .env.
#     Für SELBST erzeugte Tokens `$`-freie Erzeugung bevorzugen: `openssl rand -hex 32`
#     (nur 0-9a-f) statt `-base64` — dann entfällt das Escaping ganz.
#     Kontrolle: `docker compose -f docker-compose.yml -f docker-compose.prod.yml \
#                 config 2>&1 >/dev/null | grep -c "is not set"`  → muss 0 sein.
# ─────────────────────────────────────────────────────────────────────────────

# Öffentliche Domain (Caddy, nur lokal). Default localhost -> interne CA / self-signed TLS.
# In Produktion ungenutzt (Traefik terminiert TLS) — siehe Prod-Block oben.
MANBAN_DOMAIN=localhost
MANBAN_ACME_EMAIL=admin@example.com

# Öffentliche Basis-URL für Links in E-Mails (Verifikation). Hinter Caddy = https.
MANBAN_BASE_URL=https://localhost

# Entwicklungs- und Testbetrieb ausdrücklich einschalten: erlaubt den Start mit den
# unsicheren Standardwerten (Warnung beim Start). NUR für Entwicklung und Test.
# Für den lokalen Stack nicht nötig — docker-compose.yml setzt den Schalter bereits selbst;
# das Produktions-Overlay setzt ihn fest auf false und ignoriert diesen Eintrag hier.
#MANBAN_DEV_MODE=true

# Secret zum Signieren der Session-Tokens — bewusst LEER ausgeliefert, damit niemand
# einen Schlüssel gesetzt hat, ohne ihn gewählt zu haben.
# Leer bedeutet: Lokal greift der Standardwert aus docker-compose.yml, die Anwendung
# startet und warnt. Produktiv bricht schon der Compose-Aufruf ab, am `:?` hinter
# MANBAN_SESSION_SECRET in docker-compose.prod.yml (`:?` behandelt leer wie ungesetzt).
# In PRODUKTION zwingend einen zufälligen 32+-Byte-Wert eintragen, z. B.:
#   openssl rand -hex 32   (nur 0-9a-f, kein $-Escaping nötig — siehe Prod-Block oben)
MANBAN_SESSION_SECRET=
# Session-Cookie nur über HTTPS ausliefern. Hinter TLS (Prod) true;
# nur für reines HTTP-Lokal ggf. auf false setzen.
MANBAN_COOKIE_SECURE=true

# Einmal-Token, um auf einer frischen Instanz den ersten Plattform-Admin zu erzeugen
# (Aufruf /admin/bootstrap). Leer = deaktiviert; greift nur, solange kein Admin existiert.
# Nach dem Bootstrap wieder leeren und manban-api neu erstellen.
MANBAN_BOOTSTRAP_ADMIN_TOKEN=

# Postgres
POSTGRES_DB=manban
POSTGRES_USER=manban
POSTGRES_PASSWORD=change-me

# Verbindungspool und Server-Threads (Issue #998). Die Werte sind begründete Startwerte für
# 50 gleichzeitig aktive Personen; die Rechnung steht in src/main/resources/application.yml.
# Höchstzahl der Datenbankverbindungen der Anwendung (muss unter max_connections von Postgres bleiben).
MANBAN_DB_POOL_MAX=20
# Verbindungen, die auch ohne Last offen gehalten werden.
MANBAN_DB_POOL_MIN_IDLE=5
# Wie lange ein Aufruf höchstens auf eine freie Verbindung wartet, in Millisekunden.
MANBAN_DB_CONNECTION_TIMEOUT_MS=5000
# Höchstzahl gleichzeitig bearbeiteter HTTP-Aufrufe (Tomcat-Bearbeiter-Threads).
MANBAN_SERVER_THREADS_MAX=600

# MinIO (Objektspeicher für Anhänge, ab Issue B5)
MINIO_ROOT_USER=manban
MINIO_ROOT_PASSWORD=change-me-minio

# E-Mail-Versand (Verifikation, Passwort-Reset, Einladungen).
# false = kein SMTP nötig, Links werden nur geloggt. Für Produktion auf true setzen —
# erst dann greifen die SMTP-Zugangsdaten unten.
MANBAN_MAIL_ENABLED=false
# Absender-Adresse der System-Mails.
MANBAN_MAIL_FROM=info@mwolff.org

# SMTP-Zugang (Beispiel: Strato). Echtes Passwort NUR hier in der gitignorten .env eintragen.
MANBAN_SMTP_HOST=smtp.strato.de
# Unterstützt wird 587 = STARTTLS (Default, gegen Strato verifiziert).
# Port 465 (implizites SSL/SMTPS) wird derzeit NICHT unterstützt: die App aktiviert nur
# STARTTLS, kein mail.smtp.ssl.enable — 465 würde daher fehlschlagen. Bei 587 bleiben.
MANBAN_SMTP_PORT=587
MANBAN_SMTP_USER=info@mwolff.org
MANBAN_SMTP_PASSWORD=change-me-smtp
# SMTP-Auth bzw. STARTTLS abschalten (beides Default true) — nur für lokale Mailcatcher nötig.
#MANBAN_SMTP_AUTH=false
#MANBAN_SMTP_STARTTLS=false

# --- Zaehlbremse gegen Massenversuche (Issue #840) ---------------------------
# Begrenzt Anmeldung, Registrierung und Reset-Anforderung je Herkunft und Vorgang.
# Alle Werte haben Vorgaben; die Zeilen sind nur noetig, wenn davon abgewichen wird.
# Ganz abschalten (nur fuer Tests oder hinter einem eigenen Schutz davor):
#MANBAN_RATELIMIT_ENABLED=false
# Zahl der Proxies VOR der Anwendung. 1 passt zum mitgelieferten Caddy (Compose-Standard)
# und zu Traefik in Produktion. 0 gilt, wenn die App direkt erreichbar ist. Zu hoch gesetzt
# liest die Bremse eine vom Client mitgeschickte Adresse — dann waere sie umgehbar.
#MANBAN_RATELIMIT_TRUSTED_PROXY_COUNT=1
# Obergrenze der gleichzeitig beobachteten Herkuenfte; schuetzt den Speicher.
#MANBAN_RATELIMIT_MAX_TRACKED_ORIGINS=100000
# Je Vorgang: Versuche im Zeitfenster, Laenge des Fensters, Dauer der Sperre (ISO-8601).
#MANBAN_RATELIMIT_LOGIN_MAX_ATTEMPTS=10
#MANBAN_RATELIMIT_LOGIN_WINDOW=PT15M
#MANBAN_RATELIMIT_LOGIN_BLOCK_DURATION=PT15M
#MANBAN_RATELIMIT_REGISTER_MAX_ATTEMPTS=10
#MANBAN_RATELIMIT_REGISTER_WINDOW=PT15M
#MANBAN_RATELIMIT_REGISTER_BLOCK_DURATION=PT15M
#MANBAN_RATELIMIT_FORGOT_MAX_ATTEMPTS=10
#MANBAN_RATELIMIT_FORGOT_WINDOW=PT15M
#MANBAN_RATELIMIT_FORGOT_BLOCK_DURATION=PT15M

# --- Durchsatzbremse fuer Zugriffstoken (Issue #1002) -------------------------
# Getrennt von der Zaehlbremse oben: begrenzt die Befehle einer PERSON ueber die Token-API
# (CLI, Nachtlauf), nie die Weboberflaeche. Antwort bei Ueberschreitung: 429 mit Retry-After
# und type urn:manban:overload — das Werkzeug wiederholt dann selbst.
# Ganz abschalten (nur fuer Tests oder hinter einem eigenen Schutz davor):
MANBAN_THROUGHPUT_ENABLED=true
# Befehle je Person und Minute; kontinuierlich nachgefuellt (eins je 60/N Sekunden).
MANBAN_THROUGHPUT_PER_MINUTE=60
# Davon gleichzeitig laufend.
MANBAN_THROUGHPUT_CONCURRENT=10
# Obergrenze der gleichzeitig verfolgten Personen; schuetzt den Speicher.
MANBAN_THROUGHPUT_MAX_TRACKED_PERSONS=100000

# --- Automatiken (Aufraeumen und Zustellung) ---------------------------------
# Die Vorgaben entsprechen denen der Anwendung; ein kopiertes .env aendert das Verhalten nicht.
# Achtung: Eine Aenderung wirkt erst, wenn der Container neu erstellt wird — `docker compose up -d`.
# `docker compose restart manban-api` liest die .env NICHT neu.
# Aufraeumen erledigter Karten an/aus und nach wie vielen Tagen archiviert wird.
MANBAN_CLEANUP_ENABLED=true
MANBAN_DONE_RETENTION_DAYS=30
# Zustellung der Outbox: an/aus, Abstand der Laeufe in Millisekunden, Zahl der Versuche
# je Eintrag und wie lange erledigte Eintraege aufbewahrt werden.
MANBAN_OUTBOX_ENABLED=true
MANBAN_OUTBOX_POLL_INTERVAL_MS=5000
MANBAN_OUTBOX_MAX_ATTEMPTS=8
MANBAN_OUTBOX_RETENTION_DAYS=7

# --- Sicherung und Rueckholung (Issue #829) ----------------------------------
# Die Werte unten wirken NUR, wenn die Sicherung zugeschaltet ist:
#   docker compose -f docker-compose.yml -f docker-compose.backup.yml up -d --build
# Es gibt genau diesen einen Schalter, und er steht absichtlich nicht hier: Das Overlay meldet der
# Anwendung selbst, dass gesichert wird. Ein zweiter Schalter in dieser Datei koennte vom Zustand
# der Container abweichen — die Anwendung meldete dann "veraltet", waehrend gar kein
# Sicherungs-Container laeuft.
# Das Zuschalten erfordert einen einmaligen Neustart der Datenbank (WAL-Archivierung).
# Ausfuehrlich: docs/backup.md.
#
# Ziel der Kopie ausser Haus als rclone-Pfad "<remote>:<verzeichnis>". Das Remote wird in
# backup/rclone.conf eingerichtet (Vorlage: backup/rclone.conf.example). Der Ablageort ist
# austauschbar — Nextcloud ueber WebDAV ist eines unter vielen.
MANBAN_BACKUP_TARGET=nextcloud:manban-sicherung
# Oeffentlicher age-Schluessel, gegen den die Kopie ausser Haus verschluesselt wird.
# Erzeugen mit:  age-keygen -o manban-backup.key   (gibt den oeffentlichen Teil aus)
# ACHTUNG: Hier steht NUR der oeffentliche Teil (age1...). Den privaten Schluessel ausserhalb des
# Servers verwahren — der Server kann seine eigenen Sicherungen sonst nicht lesen, und das ist
# Absicht. Ohne den verwahrten privaten Schluessel ist aus der Kopie ausser Haus nichts zu holen.
MANBAN_BACKUP_AGE_RECIPIENT=age1beispielbeispielbeispielbeispielbeispielbeispielbeispiel
# Wie lange Basissicherungen und WAL-Archive aufbewahrt werden, in Tagen.
MANBAN_BACKUP_RETENTION_DAYS=7
# Takt des Anhang-Spiegels als ISO-8601-Dauer (PT5M = alle fuenf Minuten). Zugleich die Grenze der
# Rueckholgenauigkeit: nie weiter nach vorn als der letzte Spiegel.
MANBAN_BACKUP_MIRROR_INTERVAL=PT5M
# Takt der Basissicherung, sechsfeldrig wie in Spring (Sekunde Minute Stunde Tag Monat Wochentag).
# Der Sicherungs-Container verwirft das Sekundenfeld und faehrt die restlichen fuenf als cron.
MANBAN_BACKUP_BASE_CRON="0 0 3 * * *"
