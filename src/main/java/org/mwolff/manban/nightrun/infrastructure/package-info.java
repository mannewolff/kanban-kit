/**
 * Infrastrukturschicht (ausgehende Adapter) des Moduls {@code nightrun}.
 *
 * <p>{@code @NullMarked} (Issue #0080): Alle Referenzen in diesem Package sind per Default
 * non-null; Ausnahmen tragen explizit {@code @Nullable} (CLAUDE-java.md §6.2). Die Annotation gilt
 * nicht für Unter-Packages — {@code infrastructure.persistence} trägt eine eigene.
 */
@NullMarked
package org.mwolff.manban.nightrun.infrastructure;

import org.jspecify.annotations.NullMarked;
