/**
 * Persistenz-Adapter des Moduls {@code backup} (JPA-Entity, Spring-Data-Repository, Adapter).
 *
 * <p>{@code @NullMarked} (Issue #0080): Alle Referenzen in diesem Package sind per Default
 * non-null; Ausnahmen tragen explizit {@code @Nullable} (CLAUDE-java.md §6.2).
 */
@NullMarked
package org.mwolff.manban.backup.infrastructure.persistence;

import org.jspecify.annotations.NullMarked;
