---
id: 0029
type: task
status: done
title: U11 — Listen-Ansicht (Filter-Chips, Zeilen mit Body-Vorschau)
created: 2026-07-09
done_at: 2026-07-09
---
## Kontext
manban hat bisher keine Listen-Ansicht. Die Toolbox (`../java/frontend/src/pages/kanban/KanbanListView.tsx`, `listExcerpt.ts`) bietet eine board-weite Liste mit Status-Filter-Chips (inkl. Archiv) und pro Zeile Nummer + Status-Badge + Epic-Badge + Titel + Body-Vorschau. Ziel: wortgetreuer Port, Enum→dynamische Board-Spalten; Filter-Persistenz über localStorage (manban hat keinen Settings-Store).

## Aufgabe
- `frontend/src/pages/BoardListPage.tsx` neu (Port `KanbanListView.tsx`, Route `/boards/:boardId/list`):
  - Filter-Chips über `board.columns` + "Archiv" (Pseudo-Status); aktive Filter in localStorage.
  - Zeilen: Drag-Handle (`DragIndicatorIcon`), `#num`, Status-Chip (`statusColors(column.name)` bzw. `ARCHIVED_STATUS_COLOR`), Epic-Badge (falls parentId), Titel, Body-Vorschau (`stripMarkdown`).
  - Klick öffnet das Detail-Modal (aus U9); Reorder per nativem HTML5-D&D innerhalb derselben Spalte (wie die Toolbox-Liste).
  - Lädt Board (Spalten), Karten (inkl. archivierte — `listByBoard` liefert sie bereits) und Epics.
- `frontend/src/lib/listExcerpt.ts` (Port): `stripMarkdown`, `clampExcerptWidth` (resizable Excerpt-Spalte optional; sonst feste Breite).
- `frontend/src/layout/navItems.ts`: "Liste"-Eintrag in der Board-Gruppe; `App.tsx`: Route.
- Tests: Filter-Toggle blendet Spalten ein/aus, Zeile zeigt Status-Chip + Body-Vorschau, Klick öffnet Detail.

## Akzeptanzkriterium
- `npm test` + `npm run build` grün.
- "Liste"-Eintrag in der Sidebar; Filter-Chips schalten Spalten (und Archiv) ein/aus (in localStorage gemerkt); Zeilen zeigen Nummer, Status, Epic, Titel, Body-Vorschau; Klick öffnet das Detail-Modal.

## Abhängigkeiten
U10 (Issue #28, EpicBadge/ARCHIVED_STATUS_COLOR) und U9 (Issue #27, Detail-Modal).

---
**Kommentar** (2026-07-09 12:25)

## Abschlussbericht Issue #29

### Änderungen
- `frontend/src/pages/BoardListPage.tsx` (neu, Route /boards/:id/list) — Port von KanbanListView: Filter-Chips über board.columns + 'Archiv' (Auswahl in localStorage je Board); Zeilen mit Drag-Handle, #Nummer, Status-Chip (statusColors / ARCHIVED_STATUS_COLOR), Epic-Badge, Titel, Body-Vorschau (stripMarkdown); Klick öffnet das Detail-Modal (aus U9); Reorder per nativem D&D innerhalb derselben Spalte; canEdit aus Memberships (+Fallback)
- `frontend/src/lib/listExcerpt.ts` (+Test) — stripMarkdown portiert
- `frontend/src/layout/navItems.ts` — 'Liste'-Eintrag in der Board-Gruppe
- `frontend/src/App.tsx` — Route
- `frontend/src/pages/BoardListPage.test.tsx` — Filter (archivierte erst nach Klick), Status-Chip + Body-Vorschau, Klick öffnet Detail

### Tests und Checks
- `npm test` → 17 Dateien, 46 Tests grün
- `npm run build` (tsc strict + vite) → grün

### Hinweise
- Archivierte Karten sind über den 'Archiv'-Filter sichtbar (listByBoard liefert sie mit); ihre Wiederherstellung/Löschung ist im Detail-Modal aktuell nicht als Aktion abgebildet — falls gewünscht, ein Folge-Issue (Restore/Force-Delete im Modal).
- Resizable Excerpt-Spalte der Toolbox bewusst nicht portiert (feste 35%-Breite) — laut Plan optional.
- Filter-Persistenz über localStorage statt server-seitig (manban hat keinen Settings-Store).