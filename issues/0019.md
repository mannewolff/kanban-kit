---
id: 0019
type: task
status: done
title: U4 — Karten-Detail (Beschreibung, Kommentare, Anhänge)
created: 2026-07-09
done_at: 2026-07-09
---
## Kontext
Detailarbeit an der Karte; portiert `KanbanDetailModal`-Muster.

## Aufgabe
- Detail-Modal: Markdown-Beschreibung (GitHub-Stil).
- Kommentar-Liste/-Form.
- Anhang-Upload/-Liste/-Vorschau (PNG/JPEG/PDF/MD).
- Abhängigkeiten anzeigen.

## Akzeptanzkriterium
- Beschreibung editieren, kommentieren, Datei anhängen + Vorschau.
- Alles gegen B4/B5-API.
- Tests.
- `npm run build` grün.

## Abhängigkeiten
U3 (Issue #18), B4 (Issue #13), B5 (Issue #14).


---
**Kommentar** (2026-07-09 09:56)

## Abschlussbericht Issue #19

### Änderungen
- `frontend/src/api/comments.ts` — neues Modul: list/create/update/remove gegen /api/cards/{id}/comments bzw. /api/comments/{id}
- `frontend/src/api/attachments.ts` — neues Modul: list/upload (multipart, kein manueller Content-Type)/remove/fetchBlob gegen /api/cards/{id}/attachments und /api/attachments/{id}
- `frontend/src/api/cards.ts` — cardsApi.update ergänzt (PATCH /api/cards/{id} mit title/description/dependencies)
- `frontend/src/components/CardDetailModal.tsx` — neues Modal: Markdown-Beschreibung (react-markdown + remark-gfm) inline editierbar, Kommentarliste + Formular (Löschen nur für eigene Kommentare), Anhänge mit Upload/Löschen/Download-Links und Bild-Inline-Vorschau via Blob, Abhängigkeits-Chips
- `frontend/src/pages/BoardPage.tsx` — Modal an BoardView.onCardClick verdrahtet, reload der Karten nach Beschreibungsänderung
- `frontend/src/components/CardDetailModal.test.tsx` — 3 Vitest-Fälle (Markdown/Deps/Kommentare rendern, Kommentar anlegen, Beschreibung speichern)
- `frontend/package.json` — react-markdown ^9 + remark-gfm ^4

### Tests und Checks
- `npm test` → 7 Dateien, 15 Tests grün (12 bestehende + 3 neue)
- `npm run build` (tsc strict + vite) → grün, Bundle gebaut

### Hinweise
- Bild-Vorschau nutzt den Download-Endpunkt (Content-Disposition: attachment) nur als Blob-Quelle; die Anzeige erfolgt clientseitig über eine Object-URL, kein Inline-Rendering vom Server (Anti-XSS bleibt erhalten).
- Kommentar-Löschen wird im UI nur für eigene Kommentare angeboten; ADMIN/OWNER-Moderation erlaubt das Backend zusätzlich, ist aber (bewusst) nicht als UI-Button abgebildet — kann in U5 nachgezogen werden.
- Bundle-Größe >500 kB (Markdown-Parser); für das MVP unkritisch, Code-Splitting später möglich.