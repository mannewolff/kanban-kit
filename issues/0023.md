---
id: 0023
type: task
status: done
title: U7 — Board-UX: +Karte-Dialog, canEdit-Fix, Spaltenfarben, Ready-Spalte
created: 2026-07-09
done_at: 2026-07-09
---
## Kontext
Zwei konkrete Mängel der Board-Ansicht: (1) Karten werden nur über ein verstecktes Enter-only-Placeholder-Feld angelegt, das zudem nur erscheint, wenn `canEdit` true ist — und `canEdit` hängt an einer nachladenden Rolle (`projectsApi.list()` in `BoardPage.tsx`), was einen Race erzeugt (Feld fehlt → „kann keine Karte anlegen"). (2) Neue Boards haben die Default-Spalten [Backlog, In Progress, Review, Done] — die gewohnte Ready- und In-Review-Spalte fehlt. Zusätzlich soll die Board-Optik an die Toolbox angeglichen werden (Spaltenfarben, Kartенstil). Vorlage: `../java/frontend/src/pages/kanban/*`.

## Aufgabe
- Neu `components/NewCardModal.tsx`: schlanker Port von `../java/frontend/src/pages/kanban/KanbanNewItemModal.tsx` — zentrierter `Dialog` mit Titel (Pflicht, autoFocus) + Beschreibung (Markdown-Vorlage `## Kontext / ## Aufgabe / ## Akzeptanzkriterium / ## Abhängigkeiten`), Abbrechen/Anlegen. Kein Epic/Typ in U7 (kommt in E2).
- `BoardView.tsx`: sichtbarer „+"-Button im Spaltenkopf öffnet das Modal (ersetzt das Enter-only-Feld). Nach Anlegen Karte optimistisch einfügen.
- `api/cards.ts`: `cardsApi.create` um optionales `description` erweitern (Backend `CreateCardRequest` akzeptiert es bereits).
- `BoardPage.tsx`: `canEdit` synchron aus `useAuth().user.memberships` für `board.projectId` ableiten statt per `projectsApi.list()` nachzuladen (Race beseitigt, ein API-Call weniger).
- `lib/statusColors.ts`: von einzelner Farbe auf volles `{bg,text,dot}`-Set je Spaltenname erweitern (Palette aus `../java/frontend/src/pages/kanban/statusColors.ts`: backlog/ready/in_progress/in_review/done, Match per Namensmuster). Spaltenkopf getönt + Status-Punkt + Kartenzähler; Karten weiß mit weichem Schatten + Hover. Native HTML5-D&D bleibt.
- Backend `board/application/BoardService.java` Zeile 24: `DEFAULT_COLUMNS` → `[Backlog, Ready, In Progress, In Review, Done]`. Gilt für neue Boards; bestehende bleiben (keine Migration). Backend-Test für Default-Spalten anpassen.
- Frontend-Tests: Modal legt Karte an (inkl. Beschreibung), Spaltenkopf zeigt Farbe/Zähler, `canEdit` aus Membership korrekt.

## Akzeptanzkriterium
- `npm test` + `npm run build` grün; `mvn verify` grün (angepasster Default-Spalten-Test).
- Ein neu angelegtes Board zeigt Backlog · Ready · In Progress · In Review · Done.
- „+"-Button pro Spalte sichtbar; Klick öffnet Dialog; Anlegen fügt Karte sichtbar ein.
- Spaltenköpfe farbig nach Toolbox-Palette mit Zähler.

## Abhängigkeiten
Keine. (U6 empfohlen zuerst, aber technisch unabhängig.)

---
**Kommentar** (2026-07-09 11:21)

## Abschlussbericht Issue #23

### Änderungen
- `frontend/src/components/NewCardModal.tsx` (neu) — zentrierter Anlage-Dialog: Titel (Pflicht, autoFocus) + Beschreibung mit vierteiliger Markdown-Vorlage; Cmd/Ctrl+Enter legt an
- `frontend/src/components/BoardView.tsx` — sichtbarer +Button je Spaltenkopf öffnet den Dialog (ersetzt Enter-only-Feld); Spaltenköpfe getönt (statusColors) mit Punkt + Zähler; Karten weiß/Schatten/Hover; native D&D bleibt
- `frontend/src/api/cards.ts` — cardsApi.create um optionales description erweitert
- `frontend/src/pages/BoardPage.tsx` — canEdit synchron aus useAuth().user.memberships; Fallback auf projectsApi.list nur wenn Projekt dort (noch) unbekannt (frisch angelegt). Beseitigt den Rollen-Race
- `frontend/src/lib/statusColors.ts` — volles {bg,text,dot}-Set je Spaltenname (Toolbox-Palette) + COLUMN_SURFACE_BG
- `frontend/src/lib/statusColors.test.ts` (neu), `BoardView.test.tsx` (+Anlegen/+VIEWER), `BoardPage.test.tsx` (neu, canEdit aus Membership)
- `src/main/java/.../board/application/BoardService.java` — DEFAULT_COLUMNS → [Backlog, Ready, In Progress, In Review, Done]
- `src/test/java/.../board/BoardIT.java`, `.../card/CardMoveIT.java` — Spaltenordnung/-anzahl (5) und Done-Index nachgezogen

### Tests und Checks
- `npm test` → 12 Dateien, 28 Tests grün (22 bestehende + 6 neue)
- `npm run build` (tsc strict + vite) → grün
- `mvn verify -Dit.test=BoardIT,CardIT,CardMoveIT` → BUILD SUCCESS (6/5/5 grün)

### Hinweise
- Default-Spalten gelten nur für NEU angelegte Boards; bestehende behalten ihre Spalten (keine Datenmigration).
- Volle Backend-Suite wurde nicht erneut gefahren, sondern gezielt die von der Spaltenänderung betroffenen ITs; übrige ITs nutzen ausschließlich columns[0] (Backlog) und sind nicht betroffen (per grep verifiziert).
- canEdit-Fallback deckt den Fall ab, dass /api/me-Memberships nach Projekt-Anlage in derselben Session veraltet sind — sauberer wäre ein Auth-Refresh nach Projekt-Anlage (außerhalb dieses Issue-Scopes).