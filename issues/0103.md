---
id: 0103
type: task
status: ready
title: tbx-CLI nach kanban-kit umziehen (cli/tbx.mjs)
created: 2026-07-13
---
## Kontext
Die Kanban-CLI `tbx.mjs` liegt noch im Toolbox-Repo (`/Users/manfredwolff/ki-projects/java/cli/`), gehört aber ins kanban-kit-Repo, damit sie mit dem Produkt ausgeliefert wird und das eigene Board treiben kann (Voraussetzung für den Dogfooding-Durchstich #0104). `board.mjs` bleibt Quelle der Wahrheit im Kit. Der Contract (Config-Dir `~/.config/toolbox-cli/`, Header `X-Kanban-Token`, `issueTracker: toolbox`) bleibt **unverändert** — ein tbx-/Config-Rename ist ein separates Groß-Refactoring und nicht Teil dieses Issues.

## Aufgabe
- `cli/tbx.mjs` + `cli/tbx.test.mjs` aus `java/cli/` ins kanban-kit-Repo kopieren (Top-Level `cli/`, **committet** — nicht `.claude/`, das ist gitignored).
- `PROD_DEFAULT_HOST` von `https://toolbox.mwolff.org` → `https://kanban.mwolff.org`; Help-Text/Kommentare „Toolbox" → „kanban-kit" (nur Prosa; Config-Dir-Name bleibt `toolbox-cli`, damit der `board.mjs`-Toolbox-Adapter die Auth findet).
- Tests an neuen Host-Default anpassen.

## Akzeptanzkriterium
- `node cli/tbx.mjs --help` und `node --test cli/tbx.test.mjs` laufen grün.
- **Lauffähig ohne weiteren Repo-Kontext** (Single-File, zero-deps): als Kopie außerhalb des Repos ausführbar.
- Default-Host ist `https://kanban.mwolff.org`.

## Abhängigkeiten
Keine.