---
id: 0006
type: task
status: done
title: A4 — PAT / API-Token portieren
created: 2026-07-09
done_at: 2026-07-09
---
## Kontext
Automatisierung/CLI-Zugang ohne interaktives Login (aus Toolbox bewährt).

## Aufgabe
- `common/token`-Crypto portieren (`TokenCryptoPort`, `Sha256TokenCryptoAdapter`, Format `tk_<64hex>`).
- `kanban_access_token`-Domain + Use-Cases (create/list/revoke/resolve, Plaintext nur einmal).
- `POST/GET/DELETE /api/access-tokens` (nur per Cookie-Login verwaltbar, nicht per PAT selbst — Least Privilege).
- Auth-Filter für Header `X-Kanban-Token`, additiv neben der Cookie-Auth, baut synthetische Authentication mit `user_id`.

## Akzeptanzkriterium
- Token anlegen liefert Plaintext einmalig.
- Request mit gültigem `X-Kanban-Token` authentifiziert.
- Widerrufener Token → 401.
- Token-Verwaltung per PAT verweigert.
- `mvn verify` grün.

## Abhängigkeiten
A2 (Issue #4).


---
**Kommentar** (2026-07-09 08:39)

## Abschlussbericht Issue #6

### Änderungen
- `common/token/TokenCryptoPort.java` + `Sha256TokenCryptoAdapter.java` — Token-Crypto (tk_<64hex>, nur Hash persistiert), portiert aus Toolbox.
- `accesstoken/domain/AccessToken.java` — PAT-Aggregat.
- `accesstoken/application/AccessTokenService.java` — create/list/revoke/resolve; `AccessTokenRepository` (Port); `AccessTokenNotFoundException` (404).
- `accesstoken/infrastructure/persistence/*` — JPA-Adapter auf kanban_access_token.
- `accesstoken/infrastructure/security/PatAuthenticationFilter.java` — X-Kanban-Token → synthetische Auth (AUTH_PAT), last_used_at-Update.
- `accesstoken/web/AccessTokenController.java` — POST/GET/DELETE /api/access-tokens.
- `auth/…/SessionAuthenticationFilter` — setzt jetzt AUTH_SESSION; `SecurityConfig` — PAT-Filter eingehängt, /api/access-tokens/** verlangt AUTH_SESSION.

### Tests und Checks
- `mvn verify` → BUILD SUCCESS: 1 Unit + 21 Integrationstests (22 gesamt).
- AccessTokenIT: Anlegen liefert Klartext einmalig (tk_…); X-Kanban-Token authentifiziert /api/me; Auflisten ohne Hash/Klartext; Widerruf → PAT danach 401; unbekanntes PAT → 401; Token-Verwaltung per PAT → 403 (Least Privilege).

### Hinweise
- display_name wird vorerst = name gesetzt; ab B4 als Autor PAT-erzeugter Kommentare nutzbar.
- resolve() aktualisiert last_used_at bei jedem PAT-Request (kleiner Write on read; für MVP ok).
- SecurityConfig (auth-Modul) importiert den PatAuthenticationFilter (accesstoken-Modul) — bewusste Kopplung im Security-Kompositionswurzel.