---
id: 0084
type: task
status: ready
title: Doku: CLAUDE-react.md an manban-Realitaet und Stand 2026 angleichen
created: 2026-07-11
---
## Kontext
Review der [CLAUDE-react.md](CLAUDE-react.md) (2026-07-11): Die konzeptionellen Abschnitte (Komponenten, Hooks, State, A11y, Formulare, Routing) sind aktuell. Aber die Datei beschreibt an mehreren Stellen die **Toolbox** statt manban: Performance-Tabelle mit DashboardPage/OIDC, Code-Organisation mit `notify/`/`pages/tools/`, API-Beispiele `resize.ts`/`ogImage.ts`, `SpaForwardingController`. Der ESLint-Abschnitt beschreibt ein Gate, das erst mit dem ESLint-Issue (#0085) real wird.

## Aufgabe
Korrekturen in `CLAUDE-react.md`:
- **Performance-Budget**: Toolbox-Tabelle (DashboardPage-Widget-Registry, OIDC-Vendor-Floor, Messung 2026-05-31) durch die manban-Realität nach #0063 ersetzen (Vendor `index.js` ~407 kB / gzip ~131 kB; Route-Chunks < 10 kB; Budget 600 kB). 
- **Kleine Code-Anpassung**: `build.chunkSizeWarningLimit: 600` in `frontend/vite.config.ts` tatsächlich setzen (Doku behauptet es, real gilt Vite-Default 500) — danach stimmt die Doku-Aussage.
- **Code-Organisation**: Strukturliste auf manban: `components/` (AppShell, BoardView, CardDetailModal, …), `layout/` (nur navItems), `auth/` (AuthContext), `routes/` (ProtectedRoute), `pages/`, `lib/`, `api/`, `test/`. Toolbox-Einträge (`notify/`, `pages/tools/`, DashboardPage, SettingsPage) entfernen.
- **Datenzugriff**: Beispiel-Module `resize.ts`/`ogImage.ts` durch reale ersetzen (`boards.ts`, `cards.ts`, `projects.ts`, …); `ApiError`/`fieldErrors`-Beschreibung mit dem ProblemDetails-Issue #0081 konsistent formulieren; den `parse`-Type-Guard-Mechanismus aus #0070 dokumentieren.
- **Routing**: `SpaForwardingController` → `SpaWebConfig` (realer Klassenname).
- **ESLint-Abschnitt**: nach Umsetzung von #0085 auf die real existierende Config beziehen (echte Plugin-Liste, echte deaktivierte Regeln statt der Toolbox-Fiktion `react-hooks/set-state-in-effect`).
- **Pflichtchecks**: `npm run lint` erst nach #0085 als Pflicht listen; bis dahin build+test.
- **Neu: Abschnitt „Versionsstrategie"**: Ist-Stand (React 18.3, Vite 5.4, MUI 6.1, RR 6.28, TS 5.6, Vitest 2.1) + Zielkorridor (React 19 + React Compiler, Vite 7, MUI 7, RR 7, Vitest 3+) mit Upgrade-Bedingungen; Hinweis, dass der React Compiler manuelles `useMemo`/`useCallback` weitgehend ersetzt.
- **Neu: Hinweis** auf dokumentierte Architektur-Optionen, die einen eigenen Plan brauchen (TanStack Query für Server-State, Playwright-E2E) — als bewusst offene Entscheidungen, nicht als Pflicht.

## Akzeptanzkriterium
- `rg "resize\.ts|ogImage|DashboardPage|SpaForwardingController|notify/|pages/tools" CLAUDE-react.md` → leer.
- Strukturliste und Performance-Tabelle decken sich mit `frontend/src/` bzw. dem realen `npm run build`-Output.
- `vite.config.ts` enthält `chunkSizeWarningLimit: 600`; `npm run build` weiter grün.
- Versionsstrategie-Abschnitt vorhanden.

## Abhängigkeiten
ESLint-Abschnitt und Pflichtchecks final erst nach #0085; `ApiError`-Formulierung koordiniert mit #0081. Rest unabhängig.