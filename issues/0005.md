---
id: 0005
type: task
status: done
title: A3 — Passwort-Reset
created: 2026-07-09
done_at: 2026-07-09
---
## Kontext
Standard-Selbstbedienung, gehört zu einem Produkt.

## Aufgabe
- `POST /api/auth/forgot` (E-Mail → Reset-Token in `password_reset_token`, Mail).
- `POST /api/auth/reset` (Token + neues Passwort).
- Token einmalig, ablaufend.
- Kein User-Enumeration-Leak (immer 200 auf forgot).

## Akzeptanzkriterium
- Reset-Flow end-to-end getestet.
- Abgelaufener/benutzter Token → Fehler.
- Keine Existenz-Preisgabe.
- `mvn verify` grün.

## Abhängigkeiten
A1 (Issue #3).


---
**Kommentar** (2026-07-09 08:33)

## Abschlussbericht Issue #5

### Änderungen
- `auth/web/PasswordResetController.java`, `ForgotPasswordRequest.java`, `ResetPasswordRequest.java` — POST /api/auth/forgot (immer 200), POST /api/auth/reset (204).
- `auth/application/RequestPasswordResetService.java` — Token erzeugen + Mail, keine Enumeration.
- `auth/application/ResetPasswordService.java` — Token prüfen, neues Passwort setzen, Token verbrauchen.
- `auth/application/{PasswordResetTokenRepository,PasswordResetMailer}.java` — Ports; `InvalidResetTokenException` (400).
- `auth/domain/PasswordResetToken.java`; `AppUser.withPasswordHash(...)`; `AuthProperties.reset-ttl`.
- `auth/infrastructure/persistence/PasswordResetToken{Entity,JpaRepository,RepositoryAdapter}.java`.
- `auth/infrastructure/mail/JavaMailPasswordResetMailer.java` — Versand schaltbar (Dev: Log).
- `SecurityConfig` (forgot/reset public), `application.yml` (reset-ttl PT1H).

### Tests und Checks
- `mvn verify` → BUILD SUCCESS: 1 Unit + 18 Integrationstests (19 gesamt).
- PasswordResetIT: forgot→reset→Login mit neuem Passwort (alt→401); unbekannte E-Mail→200 ohne Versand; ungültiges Token→400; abgelaufenes Token→400 (Passwort unverändert); Einmal-Token (zweite Einlösung→400).

### Hinweise
- forgot antwortet bewusst immer 200, auch bei unbekannter E-Mail (keine Existenz-Preisgabe).
- Wie bei A1: nur der Token-Hash wird gespeichert; Reset-Link im Dev geloggt (manban.mail.enabled).
- Reset-TTL default 1h (kürzer als Verifikation), konfigurierbar.