# Sonar S6582: optional chaining in IdeaPlanningBoard (FE)

## Kontext
Sonar S6582 (MINOR) in `IdeaPlanningBoard.tsx:164` — optional chaining (`?.`) statt `&&`-Kette.

## Aufgabe
- `IdeaPlanningBoard.tsx:164`: den Ausdruck auf optional chaining umstellen.

## Akzeptanzkriterium
- S6582 weg; `npm run build`/`lint`/`test:coverage` grün.

## Abhängigkeiten
Keine.
