---
id: 0018
type: task
status: done
title: U3 — Board-Ansicht (Karten, Spalten, Drag & Drop)
created: 2026-07-09
done_at: 2026-07-09
---
## Kontext
Herzstück; portiert die reinen Kanban-Komponenten/Helfer aus Toolbox.

## Aufgabe
- Board-Spaltenansicht, Karten.
- Drag & Drop mit optimistischem Update + Revert.
- Helfer `boardOps`/`columnMeta`/`statusColors` portiert.
- Anlegen/Verschieben gegen B2/B3-API.

## Akzeptanzkriterium
- Karte anlegen, per D&D verschieben, Reload zeigt konsistenten Stand.
- Race-/Revert-Test.
- `npm run build` grün.

## Abhängigkeiten
U2 (Issue #17), B3 (Issue #12).


---
**Kommentar** (2026-07-09 09:50)

## Abschlussbericht Issue #18

### Änderungen
- Backend: `BoardService.getBoard` + `GET /api/boards/{id}` (Board+Spalten, Mitgliedschaft); BoardIT-Test.
- Frontend: `api/cards.ts`, `boards.get`; `lib/{boardOps,statusColors,columnMeta}.ts`; `components/BoardView.tsx` (D&D); `pages/BoardPage.tsx`.

### Tests und Checks
- `mvn verify` → BUILD SUCCESS (58 Backend-Tests, inkl. getBoardReturnsColumns).
- `npm run build` grün; `npm test` → 12 grün (5 neu).
- boardOps: activeCardsInColumn (archivierte raus, sortiert), applyMove (ans Ende, Original unverändert, No-op bei gleicher Spalte).
- BoardView: Karte per Drop optimistisch in Zielspalte; bei api.move-Fehler Revert in die Ausgangsspalte (Race-/Revert-Test).

### Hinweise
- Drag & Drop nativ (HTML5 draggable/onDrop), ohne Zusatzbibliothek. Drop auf eine Spalte hängt die Karte ans Ende (Cross-Column-Move). Feinsortierung per Index innerhalb einer Spalte wäre eine spätere Verfeinerung.
- Optimistisches Update + Revert: UI ändert sofort, api.move im Hintergrund; bei Fehler zurück auf den vorigen Stand. Reload holt den konsistenten Backend-Stand (B3-Reindex).
- Neuer Board-Get-Endpunkt war nötig, um ein Board per URL (/boards/:id, Reload) mit seinen Spalten zu rendern.