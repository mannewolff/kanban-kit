---
id: 0040
type: task
status: done
title: Auto-Routing bei genau einem Projekt / einem Board
created: 2026-07-09
done_at: 2026-07-10
---
## Kontext
Bei nur einem Projekt bzw. nur einem Board sind die Auswahl-Listen überflüssige Zwischenschritte. Der Nutzer will bei genau einem Element direkt durchgeroutet werden.

## Aufgabe
- `frontend/src/pages/ProjectsPage.tsx`: nach dem Laden — wenn **genau ein** Projekt existiert, per `navigate(`/projects/${id}`, { replace: true })` direkt zur Boardauswahl.
- `frontend/src/pages/ProjectBoardsPage.tsx`: wenn **genau ein** Board existiert, per `navigate(`/boards/${id}`, { replace: true })` direkt aufs Board.
- **Loop-/Sackgassen-Schutz:** Auto-Routing darf den Nutzer nicht einsperren (sonst käme man nie zur Liste zurück, um ein 2. Projekt/Board anzulegen). Die Rücksprung-Links ("← Projekte" bzw. "← Boards") setzen ein Navigations-Signal (z. B. `state: { stay: true }` oder Query `?stay=1`), das das Auto-Routing auf der Zielseite unterdrückt. Ebenso soll die Sidebar-Navigation zur Liste nicht sofort wieder wegrouten.
- Auto-Routing nur beim regulären Aufruf, nicht während des Ladens (erst nach vorhandener Antwort entscheiden; kein Flackern/Doppel-Redirect).
- Tests: bei 1 Projekt Redirect auf /projects/:id; bei mehreren keine Umleitung; bei 1 Board Redirect auf /boards/:id; mit `stay`-Signal keine Umleitung (Liste bleibt sichtbar).

## Akzeptanzkriterium
- `npm test` + `npm run build` grün.
- Login mit genau 1 Projekt landet direkt in der Boardauswahl; genau 1 Board landet direkt auf dem Board; über den Zurück-/Sidebar-Weg bleibt die Liste erreichbar (um weitere anzulegen), ohne sofortiges Zurückrouten.

## Abhängigkeiten
Keine.

---
**Kommentar** (2026-07-09 15:55)

## Abschlussbericht Issue #40

### Änderungen
- `frontend/src/pages/ProjectsPage.tsx` — bei genau 1 Projekt navigate(replace) zu /projects/:id (state.autoRoute für die Kette)
- `frontend/src/pages/ProjectBoardsPage.tsx` — bei genau 1 Board navigate(replace) zu /boards/:id
- Loop-Schutz: Auto-Routing nur wenn location.key==='default' (Erst-Aufruf) ODER state.autoRoute (Kette). Push-Navigation (Sidebar/Zurück/Klick) hat weder → zeigt die Liste
- Tests: je ein Fall '1 Projekt → Boardauswahl' bzw. '1 Board → Board'; bestehende Mehrfach-Fälle unverändert

### Tests und Checks
- `npx vitest run` → 21 Dateien, 58 Tests grün
- `npm run build` → grün

### Hinweise
- Kein State-Threading über Zurück-Links nötig: der Trick ist, dass Erst-Aufruf (key 'default') und die Auto-Route-Kette (state) erlaubt sind, bewusste Pushes aber nicht. So bleibt die Liste über Sidebar/Zurück/Projekt-Klick erreichbar.

---
**Kommentar** (2026-07-09 16:15)

## Nachtrag: Bugfix (Commit 6c8873b)

Beim Test gemeldet: nach Login mit genau einem Projekt bleibt man auf der Projektliste.

Ursache: LoginPage navigierte mit `navigate('/')` — ein Push (location.key != 'default'), ohne autoRoute-State. Damit war keine der beiden Auto-Routing-Bedingungen erfüllt.

Fix: `navigate('/', { replace: true, state: { autoRoute: true } })` — startet die Auto-Routing-Kette (Projekt → Board) direkt nach dem Login. Test prüft die Navigations-Argumente.

Checks: npx vitest run → 62 grün; npm run build → grün.