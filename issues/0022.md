---
id: 0022
type: task
status: ready
title: U6 — Toolbox-Shell (einklappbare Sidebar) + Petrol-Theme
created: 2026-07-09
---
## Kontext
Das Frontend hat aktuell nur eine schlichte Topbar-Shell (`components/AppShell.tsx`) und ein grünes Theme. Vorlage ist die Toolbox (/Users/manfredwolff/ki-projects/java/frontend, gleicher Stack React18/MUI6/emotion/react-router6): permanente, einklappbare linke Seitenleiste, fixe Kopfleiste, Petrol-Theme. Ziel ist die vertraute Toolbox-Anmutung statt nacktem MVP.

## Aufgabe
- `components/AppShell.tsx` neu bauen nach Vorlage `../java/frontend/src/layout/AppShell.tsx`: permanenter MUI-`Drawer` 240 ↔ 56 px, Collapse-Zustand in `localStorage` (Key `sidebar-collapsed`), Ein-/Ausklapp-Button unten, Tooltips im eingeklappten Zustand. Fixe `AppBar` (`zIndex drawer+1`) mit Wortmarke „manban", Nutzer-Avatar (Initiale aus `displayName`), Logout.
- `theme.ts` auf Petrol umstellen: `primary #3d8a98` (light `#6cb4c3`, dark `#256270`), `background.default #f7f8fa`, `shape.borderRadius 8`.
- Neu `layout/navItems.ts` + kontextbewusste Sidebar: oben „Projekte" (→ `/`); ist ein Board offen (Route `/boards/:id`), eine nach dem Board benannte Gruppe mit Kind „Board" (den Eintrag „Epics" ergänzt E2). Board-ID aus Route, Board-Name via vorhandenem `boardsApi.get`.
- Neue Dependency `@mui/icons-material` in `frontend/package.json`.
- `App.tsx`: Shell-Einbindung an neue AppShell anpassen (AppShell rendert `<Outlet/>` selbst, analog Toolbox).
- Tests (`AppShell.test.tsx`): Shell rendert Marke + Nav, Collapse-Toggle schreibt/liest localStorage, aktiver Nav-Eintrag ist markiert.

## Akzeptanzkriterium
- `npm run build` (tsc strict + vite) grün, `npm test` grün.
- Sidebar links sichtbar, ein-/ausklappbar; Zustand überlebt Reload (localStorage).
- AppBar zeigt Wortmarke, Avatar-Initiale, Logout; Petrol-Farbe aktiv.
- In einer Board-Route erscheint die board-benannte Nav-Gruppe mit „Board".

## Abhängigkeiten
Keine.