---
id: 0001
type: task
status: ready
title: F1 — Repo-Gerüst, Build, Docker-Compose, CI
created: 2026-07-09
---
## Kontext
Ein konsolidiertes Schema statt portierter MariaDB-Inkremente (siehe Konzept §3). Frische Postgres-Baseline.

## Aufgabe
- `V1__baseline.sql` mit allen Tabellen aus §3 (inkl. `user_mfa_credential` als reines Schema, `permission`/`role_permission` mit Seed der festen Matrix).
- Postgres-Idiome konsequent: `GENERATED … STORED`, `CASE` statt `IF()`, `GENERATED BY DEFAULT AS IDENTITY`, `TIMESTAMP(6)`, separate `CREATE INDEX`, Trigger für `updated_at`.
- Leere Domain-Records + Ports je Modul angelegt.
- Testcontainers-Postgres-Setup.

## Akzeptanzkriterium
- Flyway migriert gegen Testcontainers-Postgres fehlerfrei.
- `role_permission` enthält die Seed-Matrix.
- Ein Repository-Integrationstest liest/schreibt eine Tabelle.
- `mvn verify` grün.

## Abhängigkeiten
F1 (Issue #1).