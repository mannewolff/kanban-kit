---
id: 0047
type: task
status: in_review
title: README: Getting-Started fürs öffentliche Repo
created: 2026-07-10
---
## Kontext
Das Repository ist öffentlich, hat aber noch keine README. Wer es klont, findet keine Einstiegsanleitung. Es braucht eine README, die den schnellsten Weg „von 0 auf laufendes kanban-kit" beschreibt.

## Aufgabe
- `README.md` im Repo-Root anlegen. Inhalt (knapp, aber vollständig):
  - Kurzbeschreibung: kanban-kit — self-hostbares Kanban-Board (Trello-Alternative); Projekte/Boards/Epics/Anhänge/RBAC.
  - Voraussetzungen: Docker-Laufzeit (macOS: Colima — `colima start`).
  - Start: `git clone …`, im Repo `docker compose up --build -d` (Rolle von `--build` erklären), dann `https://localhost` öffnen (selbst-signiertes Zert → einmal bestätigen).
  - Hinweis: DB-Schema wird automatisch per Flyway migriert (kein manuelles SQL nötig).
  - Ersten Admin einrichten: kurzer Verweis auf Bootstrap-Token bzw. SQL (Details in docs/betrieb.md).
  - Verweis auf die Doku (docs/ bzw. VitePress in docs-site).
- Produktname durchgängig „kanban-kit".

## Akzeptanzkriterium
- README.md vorhanden; ein Leser kann ohne Vorwissen dem Ablauf folgen (clone → build → up → öffnen → erster Admin).
- Befehle stimmen mit docker-compose.yml und docs/betrieb.md überein.

## Abhängigkeiten
Keine.

---
**Kommentar** (2026-07-10 12:44)

## Abschlussbericht Issue #47

### Änderungen
- `README.md` (neu) — Kurzbeschreibung, Voraussetzungen (Docker/Colima), Schnellstart (clone → docker compose up --build -d → https://localhost), Flyway-Hinweis, ersten Admin einrichten (Kurzfassung + Verweis docs/betrieb.md), Doku-Übersicht + docs-site.

### Tests und Checks
- Befehle gegen docker-compose.yml verifiziert: Services postgres/manban-api/caddy, MANBAN_DOMAIN default localhost, psql-User/DB = manban/manban.
- Konsistent mit docs/betrieb.md (Start, erster Admin).

### Hinweise
- Produktname durchgängig „kanban-kit" (Repo/Java-Package bleiben technisch manban).