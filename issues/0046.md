---
id: 0046
type: task
status: in_review
title: Dogfooding-Wiring: board.mjs Toolbox-Adapter + Doku
created: 2026-07-10
---
## Kontext
Mit der Kanban-Compat-API (#45) kann kanban-kit als Issue-Tracker dienen. Das aktuelle `board.mjs` im claude-workflow-kit besitzt bereits einen `ToolboxIssueTracker` (issueTracker: github|gitlab|local|toolbox). Manbans mitgelieferte Kopie unter `.claude/kit/board.mjs` ist älter und kennt `toolbox` noch nicht. Für das „Eat your own dog food" muss die aktuelle board.mjs eingezogen und der Anschluss dokumentiert werden. Der tatsächliche Umzug (issueTracker auf `toolbox` stellen) bleibt ein bewusster manueller Schritt NACH diesem Issue.

## Aufgabe
- `.claude/kit/board.mjs` auf den aktuellen Kit-Stand (`/Users/manfredwolff/ki-projects/claude-workflow-kit/kit/board.mjs`) bringen — inkl. `ToolboxIssueTracker` und der `toolbox`-Auswahl (Sync-Richtung Kit → Projekt, wie im Datei-Header vermerkt).
- workflow.config.json NICHT umstellen (bleibt `local`), aber ein dokumentiertes Beispiel ergänzen, wie auf `toolbox` mit `toolbox.host` umgestellt wird.
- Doku-Seite in `docs/` (+ Sidebar in docs-site): „kanban-kit als eigenes Board anbinden" — gebundenes Token in der UI/API erzeugen, `tbx auth login --host … --token tk_…`, `issueTracker: toolbox` setzen, Smoke-Test `node .claude/kit/board.mjs issue list`.

## Akzeptanzkriterium
- `node .claude/kit/board.mjs issue list` akzeptiert `issueTracker: toolbox` ohne „Unbekannter issueTracker".
- Der lokale Modus funktioniert unverändert (`node .claude/kit/board.mjs issue list --status ready` liefert weiter die lokalen Issues).
- Doku beschreibt den vollständigen Anschluss reproduzierbar.

## Abhängigkeiten
#45 (Kanban-Compat-API).

---
**Kommentar** (2026-07-10 12:43)

## Abschlussbericht Issue #46

### Änderungen
- `.claude/kit/board.mjs` — auf aktuellen Kit-Stand gebracht (977→1148 Zeilen), inkl. ToolboxIssueTracker + toolbox-Auswahl. **Lokal/gitignored** (.claude/ ist ausgeschlossen), daher nicht Teil des Commits.
- `docs/dogfooding.md` — reproduzierbare Anleitung: Board-gebundenes Token (curl), tbx auth login, issueTracker: toolbox, Smoke-Test, Status↔Spalte-Mapping.
- `docs-site/.vitepress/config.ts` — Nav + Sidebar-Eintrag „Dogfooding".

### Tests und Checks
- node .claude/kit/board.mjs issue list --status ready → lokaler Modus unverändert (liefert Ready-Issues).
- grep bestätigt: case "toolbox" + Fehlermeldung "… | toolbox" im neuen board.mjs.
- Dev-Server: /dogfooding.html → HTTP 200.

### Hinweise
- workflow.config.json bleibt bewusst auf `local` (der eigentliche Umzug auf toolbox ist der manuelle Schritt nach diesem Batch).
- Es wurde KEIN eigener ManbanIssueTracker gebaut: der bestehende toolbox-Adapter erfüllt das Ziel (siehe #0021-Kommentar).