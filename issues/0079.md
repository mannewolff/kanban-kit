---
id: 0079
type: task
status: ready
title: Doku: CLAUDE-java.md an manban-Realitaet und Stand 2026 angleichen
created: 2026-07-11
---
## Kontext
Review der [CLAUDE-java.md](CLAUDE-java.md) (2026-07-11) gegen den realen Ist-Zustand nach den Qualitäts-Issues #0071–#0077: Der Kern (TDD §2, Testpyramide §3, Mocking §4, 100-%-Gates §5) ist aktuell und seit den Gates auch build-technisch wahr. Aber die Doku enthält noch Toolbox-Fremdreste, tote Links und Drift zur realen `pom.xml` — und zwei hart erkaufte Lektionen aus der Umsetzung fehlen.

## Aufgabe
Korrekturen in `CLAUDE-java.md`:
- §6.3 (Z. 177): toten Verweis auf `GlobalExceptionHandler.java` (`org/mwolff/api/...`, Toolbox) ersetzen — Regel auf den künftigen ProblemDetails-Handler beziehen (Folge-Issue) bzw. bis dahin auf den Ist-Zustand (`@ResponseStatus`-Exceptions).
- §5.2: Toolbox-Beispielklassen (`JpaKanbanAdapter`, `IngestTokenAuthFilter*`, `CsvBulkParser*`, `ImageIoThumbnailer`, `IngestRateLimiter`) durch die realen manban-Ausschlüsse ersetzen (`ManbanApplication`, `*Entity`, `*JpaRepository`, `*RepositoryAdapter`/`Jdbc*`, Mail-/S3-Adapter, `SecureTokens`) und die real existierende `@ExcludeFromJacocoGeneratedReport`-Annotation (§5.4) referenzieren.
- 5 tote Links `CLAUDE-workflow.md` → `.claude/CLAUDE-workflow.md` (Z. 5, 214, 229, 252, 269).
- §1: „sobald angelegt" bei `infra/toolchains.xml.example` streichen (existiert seit #0076); Abschnitt „Lokale Build-Umgebung" ergänzen: Colima/Testcontainers-Erfordernisse (`DOCKER_HOST=unix://$HOME/.colima/default/docker.sock`, `TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock`, `~/.docker-java.properties` mit `api.version=1.44`).
- §6.2: Lombok-Absatz auf „Kein Lombok — Records, final, Konstruktoren" umstellen (Projekt hat keine Lombok-Dependency).
- §5.3: `RETURN_VALS` aus der Mutatorenliste streichen (existiert in PIT 1.17+ nicht mehr; ist in die bereits gelisteten `*_RETURNS` aufgeteilt).
- §5.1: kodifizieren, dass das PIT-Gate im Maven-Profil `-Ppit` läuft (local-check + CI), und Test Strength präzisieren („via mutationThreshold=100 miterzwungen"; pitest-maven hat keine separate Schwelle).
- §6.4: Behauptung „geteilte Container-Instanz über AbstractIntegrationTest" an die Realität anpassen (aktuell `@Container @ServiceConnection` pro Testklasse) bzw. nach Umsetzung des Container-Sharing-Issues entsprechend formulieren.
- §7: auf die versionierten Regelsets `config/checkstyle|pmd|spotbugs` verweisen; dokumentierte Ausnahmen benennen (`-Xlint:all,-serial`, SpotBugs `EI_EXPOSE_REP`-Excludes).
- Neu dokumentieren: ArchUnit-Surefire-Falle (Lektion aus #0071: `@ArchTest`/`@AnalyzeClasses` lief als „0 Tests" = falsches Grün → Regeln als reguläre `@Test` gegen `ClassFileImporter` schreiben).
- Neu: kurzer Abschnitt „Versionsstrategie" (aktuell Java 21 LTS + Spring Boot 3.5; Zielkorridor Java 25 LTS / Spring Boot 4.x / JUnit 6 mit Upgrade-Bedingungen).
- Kurzer Verweis auf Supply-Chain-Prüfung (Renovate/SBOM) → gehört inhaltlich in CLAUDE-security.md, hier nur Querverweis.

## Akzeptanzkriterium
- Kein Vorkommen von Toolbox-Klassennamen/-Pfaden mehr in `CLAUDE-java.md` (`rg "org/mwolff/api|JpaKanban|IngestToken|CsvBulk|ImageIo|IngestRateLimiter" CLAUDE-java.md` leer).
- Alle Links in der Datei zeigen auf existierende Dateien.
- §5.2-Beispiele decken sich mit den realen `pom.xml`-Ausschlüssen; §5.3 ohne `RETURN_VALS`; §5.1 beschreibt das `-Ppit`-Gate.
- ArchUnit-Falle, Build-Umgebung und Versionsstrategie sind als Abschnitte vorhanden.

## Abhängigkeiten
Keine (reine Doku). Formulierungen zu ProblemDetails/Container-Sharing so wählen, dass sie mit den Folge-Issues #0081/#0083 konsistent sind.