---
id: 0015
type: task
status: done
title: B6 — Cleanup-Job (Done-Retention)
created: 2026-07-09
done_at: 2026-07-09
---
## Kontext
Aufgeräumtes Board; Muster aus Toolbox.

## Aufgabe
- Retention pro Board oder Projekt (`doneRetentionDays`).
- `@Scheduled`-Job archiviert abgelaufene Done-Karten.
- Self-Invocation-Falle vermeiden (`@Transactional` sauber trennen).

## Akzeptanzkriterium
- Job archiviert nur abgelaufene Done-Karten.
- Abschaltbar per Property.
- Test mit fixer Zeitquelle.
- `mvn verify` grün.

## Abhängigkeiten
B3 (Issue #12).


---
**Kommentar** (2026-07-09 09:32)

## Abschlussbericht Issue #15

### Änderungen
- `card/application/DoneRetentionService.java` — archiveExpiredDoneCards(now, retentionDays), @Transactional.
- `card/infrastructure/DoneRetentionJob.java` — @Scheduled(cron), ruft den Service (separater Bean → keine Self-Invocation); @ConditionalOnProperty(manban.cleanup.enabled).
- `card/application/CleanupProperties.java` — enabled/doneRetentionDays/cron.
- `CardRepository.findArchivableDoneCards` + JPA-Query/Adapter; `ManbanApplication` @EnableScheduling; `application.yml` manban.cleanup.

### Tests und Checks
- `mvn verify` → BUILD SUCCESS: 1 Unit + 56 Integrationstests (57 gesamt).
- DoneRetentionIT (fixe Zeitquelle Instant.parse): 40-Tage-alte Done-Karte → archiviert; 5-Tage-alte Done-Karte und nicht-Done-Karte → unverändert; Rückgabe archivedCount=1.

### Hinweise
- Self-Invocation-Falle vermieden: Job (@Scheduled) und @Transactional-Service sind getrennte Beans; der Job ruft den Service über die injizierte Referenz (Proxy) auf.
- Abschaltbar: manban.cleanup.enabled=false → der Job-Bean wird nicht erzeugt (@ConditionalOnProperty).
- Retention ist aktuell global (manban.cleanup.done-retention-days). 'Pro Board/Projekt' wäre eine spätere Erweiterung (Spalte doneRetentionDays am board/project + pro-Board-Query); das F2-Schema hat dafür noch keine Spalte. Kern (Zeit-basierte Archivierung, Toggle, fixe Zeit im Test) ist erfüllt.